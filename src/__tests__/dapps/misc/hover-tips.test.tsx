describe("Hover tips", () => {
  it("should show hover message", () => {
    expect(true).toBe(false);
  });
  it("should not show hover message when disabled", () => {
    expect(true).toBe(false);
  });
});
